 1 ) This Streamlit application collects a user's name and message, validates the input, displays a success or error message, and estimates the approximate token usage based on the message length. It demonstrates basic user input handling, conditional logic, and interactive UI development using Streamlit. ( app1.py )
 
2 ) Basically, in the AI multiverse setup, you just setup the Google Gemini SDK once. Then in your prompt, you give the AI different personas like "act as a comedian" or "act as a serious coach". After that, you chat with whichever persona you want and the same AI replies in totally different tones based on that role.
( app2.py )
